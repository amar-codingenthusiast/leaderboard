import styles from "../Styles/Footer.module.css";

const Footer = () => {
	return (
		<div className={styles.container}>
			<p>Get your name on leaderboard and win exciting prizes!</p>
		</div>
	);
};

export default Footer;
